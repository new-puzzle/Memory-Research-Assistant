# AI Model API Keys (at least one required)
ANTHROPIC_API_KEY=sk-anxxxxxxxxx
TOGETHER_API_KEY=xxxxx
DEEPSEEK_API_KEY=sk-b9cdxxxxxxxxxx

MISTRAL_API_KEY=xxxxx

# Default AI Model (claude, together, deepseek, mistral)
DEFAULT_AI_MODEL=claude

# Google Cloud Configuration
GOOGLE_CLIENT_ID=4649468888888XXXXXXXXX
GOOGLE_CLIENT_SECRET=GOCSXXXXXXXXX
GOOGLE_SERVICE_ACCOUNT_JSON=./service-account.json
GOOGLE_CLOUD_PROJECT_ID=resonance-472209

# Application Settings
SECRET_KEY=your_random_secret_key_here_change_in_production
ENVIRONMENT=development
DEBUG=True

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60

# Optional: Basic Auth for Additional Security
BASIC_AUTH_USERNAME=admin
BASIC_AUTH_PASSWORD=change_this_password

# Optional: Allowed Email Domains for OAuth (comma-separated)
ALLOWED_EMAIL_DOMAINS=gmail.com,yourdomain.com

# Redis (optional - for production rate limiting)
REDIS_URL=redis://localhost:6379